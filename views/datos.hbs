<table class="table">
    <thead>
        <tr>
        <th scope="col">Id</th>
        <th scope="col">Titulo</th>
        <th scope="col">precio</th>
        <th scope="col">imagen</th>
        </tr>
    </thead>
    <tbody>
        {{#if products}}
            {{#each products}} 
                <tr>
                <td>{{this.id}}</td>
                <td>{{this.title}}</td>
                <td>$ {{this.price}}</td>
                <td><img src="{{this.url}}" alt="producto"></td>
                </tr>
            {{/each}}
        {{/if}}
    </tbody>
</table>

<form method="GET" action="http://localhost:8080/static">
    <button type="submit" class="btn btn-primary">Volver</button>
</form>